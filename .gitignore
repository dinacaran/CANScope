# ── Claude ────────────────────────────────────────────────────────────────
.claude
.claude/
*.log
docs_int/
claude.md
CLAUDE.md
docs/Internals/
TODO.md

# ── Diagnostics feature — developed in the private repo only ──────────────
core/diagnostics/
gui/diagnostics/
config/diagnostics/
tests/test_config_loader.py
tests/test_engine.py
tests/test_evidence.py
tests/test_reload_and_run.py
tests/test_rules.py
tests/diagnostics/

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
pip-wheel-metadata/
*.whl
# ── Virtual environments ──────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
.env
# ── PyInstaller ───────────────────────────────────────────────────────────
# Keep CANScope.spec in source control; exclude build artefacts
*.spec.bak
# ── IDE / editors ─────────────────────────────────────────────────────────
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
# ── OS artefacts ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
# ── CAN Scope runtime files ───────────────────────────────────────────────
canscope_dev.log
canscope_config.json
canscope_temp_plot_config.json
# Per-run diagnostic telemetry JSON (logs/diagnostics/ is created at runtime).
logs/
*.blf
*.mf4
*.mdf
*.asc
*.dbc
# ── Test / coverage ───────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
*.coveragerc
# ── Test fixtures ─────────────────────────────────────────────────────────
# sample.blf and sample.asc are generated locally by tests/fixtures/_generate.py
# and intentionally NOT committed (already caught by *.blf / *.asc above).
# sample.dbc is a hand-written text fixture that IS committed — un-exclude it.
!tests/fixtures/sample.dbc
# ── Secrets ───────────────────────────────────────────────────────────────
*.pem
*.key
.env.*
secrets.json

# ── Others ───────────────────────────────────────────────────────────────
.claude/worktrees/
*.bak
*.xlsx
*.csv
AGENTS.md
